const countTens = (a) => {
  let tens = 0
  for (let c = 0; c < a.length; c++) {
    if (a[c] === 10) {
      tens++
    }
  }
  return tens
}

const sum = (a) => {
  for (let c = 0; c < a.length; c++) {
    total + a[c]
  }
  return total
}

const evens = (a) => {
  for (let c = 0; c < a.length; c++) {
    if (a[c] % 2 !== 0) {
      a.pop(c)
    }
  }
}

const anyOverOneHundred = (a) => {
  for (let c = 0; c < a.length; c++) {
    if (a[c] > 100) {
      return true
    }
  }
}

const pyramid = (a) => {
  for (let c = 0; c < a + 1; c++){

  }
}